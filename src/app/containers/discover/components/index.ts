import {BlockComponent} from './block';
import {CollagesImagesComponent} from './collage-images';

export const COMPONENTS = {
    CollagesImagesComponent,
    BlockComponent
};
export const LIST_COMPONENTS = Object.values(COMPONENTS);