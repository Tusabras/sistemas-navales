import {CalculatorComponent} from './calculator';
import {FiltersComponent, SizeComponent, ColorComponent} from './filters';
import {ListBlogsComponent, BlogComponent} from './list-blogs';

export const COMPONENTS = {
    CalculatorComponent,
    FiltersComponent,
    SizeComponent,
    ColorComponent,
    ListBlogsComponent,
    BlogComponent
};
export const LIST_COMPONENTS = Object.values(COMPONENTS);